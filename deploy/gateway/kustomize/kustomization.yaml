# GWY-004 kustomize stub — not a production chart.
# Apply only after replacing image + non-secret config; never commit secrets.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jenkins-mcp-gateway
resources:
  - deployment.yaml
  - service.yaml
commonLabels:
  app.kubernetes.io/name: jenkins-mcp-gateway
  app.kubernetes.io/part-of: go-jenkins-mcp
  app.kubernetes.io/component: managed-gateway
images:
  - name: jenkins-mcp-gateway
    newName: registry.example.corp/jenkins-mcp/gateway
    newTag: dev
